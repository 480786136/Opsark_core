1. 发现阶段先复用已确认的项目路径与文件内容；缺少目录证据时可用 files.get_structure 或等价只读命令，再读取与当前决策有关的 README、依赖声明、示例配置或构建入口。已知参数的 Shell、变更步骤与 read_batch 文件工具可在同一有序计划中逐项执行，例如克隆并独立验收成功后读取确定路径；前置失败停止后续步骤，仍逐步进行风险审批。多项文件读取可合并规划，依赖上一项未知输出的路径不得预先猜测。路径不存在只是观察事实，不等于工具故障或部署完成；权限和连接错误不能当作路径不存在。不要默认读取完整锁文件或重复扫描整个仓库。目录名、文件名或 server.basic_info 只证明候选事实，不能证明技术栈与部署需求。
